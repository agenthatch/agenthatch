# {{ agent_name }} runtime configuration
# Generated by agenthatch
#
# API Key: auto-inherited from agenthatch global config (~/.agenthatch/config.toml)
# No manual setup needed. Use /config in the CLI to switch providers/models.

[llm]
provider = "{{ llm_provider | default('openai') }}"
model = "{{ model | default('gpt-4o') }}"
base_url = "{{ base_url | default('https://api.openai.com/v1') }}"
# api_key = ""  # Auto-inherited; uncomment to override with env var or plaintext key

[compact]
enabled = true
threshold_tokens = 8000
compact_ratio = 0.75