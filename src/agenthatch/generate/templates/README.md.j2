# {{ display_name }}

{{ description }}

Generated by [agenthatch](https://github.com/agenthatch/agenthatch).

## Quick Start

```bash
# Run interactive mode via agenthatch
agenthatch run {{ agent_name }}
```

## Configuration

Edit `runtime.toml` to configure your LLM provider, model, and API key.
Use `/config` in the interactive TUI to change settings at runtime.